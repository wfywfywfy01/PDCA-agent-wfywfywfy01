# 早会待办（第 8 节）· 规格冻结 2026-09-20

老板拍板（2026-09-20）：

1. **位置**：三追每版正文末尾新增第 8 节「今日早会待办」。
2. **分组**：`scope=group` 的条目只发给对应达标群（Viki 群→尤文静、Lina 群→丽娜&冯磊、新人小组群→邓琳莹）；
   `scope=mgmt` 的条目（经销商三步骤、JIM、春梅、付汪洋、张倩）**只进管理名单那版**（付汪阳 13365 / 徐华俊 13102 / 刘春梅 12564），不往达标群丢。
3. **数据来源（暂定）**：老板每天把早会截图发我 → 我用本地 Qwen OCR（图像任务只用 qwen3.8-27b）→ 按人填 `app/meeting_todos.json`。
   自动识别（谁发的/几点发）以后再钉死，先人工填更稳。
4. **缺数据**：当天没有条目就**不出第 8 节**（不写「无」、不编造）；条目保持原文用词，不总结不翻译。
5. **实现位置**：`app/duzhan.py` 渲染侧读 `meeting_todos.json`，按群切 `scope=group` 条目；管理版由证据 HTML / 早报侧附加（同一份数据源）。
6. **待办**：渲染 + 测试 + 上线；上线前先出草稿给老板确认。
