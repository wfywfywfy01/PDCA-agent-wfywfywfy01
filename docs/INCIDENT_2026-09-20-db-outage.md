# 事故复盘：2026-09-20 生产数据库主机冻结

## 现象

- 工作台所有依赖数据库的接口返回 **500**，`/health` 返回 **503**（降级）。
- 静态页面仍可打开（77ms），说明应用进程本身存活。
- 当天日志出现 **1693 次** `OperationalError ... timeout expired`，此前数天为 **0**。

## 根因

**数据库主机 10.100.0.176 在应用层冻结**（该主机同时是 Caddy/Docker 宿主）：

| 探测 | 结果 |
|------|------|
| TCP 22/80/443/5432 | 可建立连接（52–62ms） |
| SSH banner | 读不到（连接被中断） |
| Caddy HTTP (80) | 无响应 |
| Docker API (2375) | 无响应（HTTP 超时） |
| PostgreSQL 协议握手 | 10s 超时 |
| 随机端口 12345/54321/65000 | 超时（排除“全端口假开放”） |

结论：内核/网络栈仍在应答 TCP，**用户态进程（sshd、caddy、dockerd、postgres）全部无响应**，
典型于宿主 OOM、宿主机挂起（快照/备份）、磁盘 I/O 完全停滞。**应用侧无法自愈，需要重启该主机。**

## 放大因素（真正的可预防点）

1. **备份长期失败，导致没有恢复点**：
   远端库是 PostgreSQL **18.4**，本机 pg_dump 只有 **16.9 / 12.18**，
   每天 06:00 的 `backup_database()` 直接报 `aborting because of server version mismatch`，
   `pdca-workbench/data/backups/` 为空（最近一份还是 6 月）。
2. **故障发现依赖工作台自身调度**：17:15 开始故障，17:30 才由定时任务触发告警；
   若工作台进程本身挂掉，就完全没人报信。
3. **UI 静默降级**：接口 500 时导航里的用户区与管理入口无声消失，用户不知道发生了什么（已修复）。

## 已实施的防护

| 措施 | 落点 | 说明 |
|------|------|------|
| 数据库守护 | `scripts/db_guard.ps1` + 计划任务 `PDCA-DBGuard`（每 5 分钟） | 协议层探测（避免“端口开着但服务卡死”误判）；故障/恢复**状态变化时**推送 IM 告警；数据库恢复而工作台未起来时**自动拉起并复验** |
| 版本无关逻辑备份 | `scripts/pg_logical_backup.py` + 计划任务 `PDCA-LogicalBackup`（每日 06:30） | 用 psycopg2 逐表导出 gzip JSONL，**不依赖 pg_dump 版本**，规避 18.4 vs 16.9 不匹配；保留最近 14 份 |
| UI 明确降级 | `AppNav.vue` | 身份接口失败时显示黄色提示条 + 重试，并说明管理入口为何隐藏 |

## 恢复步骤（主机重启后）

1. `PDCA-DBGuard` 会在 5 分钟内探测到数据库恢复，**自动重启工作台并推送“已恢复”通知**；
2. 人工核验：`/health` 返回 200、`/api/auth/me` 返回 200、`/metrics` 正常；
3. 跑一次 `python scripts/pg_logical_backup.py` 确认备份链路可用；
4. 记录本次事故到 `PROGRESS.md`。

## 仍待办

- [ ] 安装 PostgreSQL 18 客户端工具（`pg_dump 18`），恢复原生 `pg_dump` 备份能力；
- [ ] 评估把生产库迁到本机 PostgreSQL 16（已安装并运行）或带监控的内网主机，消除单点；
- [ ] 给引擎加 `connect_timeout=3`，避免数据库卡死时请求堆积（需重启，待数据库恢复后部署）；
- [ ] 把 `PDCA_ALERT_WEBHOOK_URL` 或 IM 告警纳入常态巡检清单。
